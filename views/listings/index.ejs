<% layout("layouts/boilerplate.ejs") %>

<h1 style="margin-left: 1rem;">Wanderlust</h1>
<div class="row row-cols-lg-3 row-cols-md-2 row-cols-sm-1">
    <% for(let list of lists) { %> 
        <a href="/listings/<%= list._id %>" class="listing-links">
        <div class="card col">
            <p class="title"> <b><%= list.title %></b></p>
            <img 
                src="<%= list.image.url %>" 
                class="card-img-top" 
                alt="listing_image"
            />
            <div class="card-img-overlay"></div>

            <div class="card-body">
                <div class="card-text">
                    <p class="price">₹<%= list.price.toLocaleString("en-IN") %> for 2 nights</p>
                    <p class="rating">⭐ <%= list.rating || "4.9" %></p>
                </div>
            </div>
        </div>

        </a>
    <% } %>
</div>


    <!-- <div class="cards-container">
        <% for(let list of lists) { %> 
            <a href="/listings/<%= list._id %>" class="card">

                <div class="card-img">
                    <img src="<%= list.image %>" alt="<%= list.title %>">
                    <span class="badge">Guest favourite</span>
                    <i class="fa-regular fa-heart wishlist"></i>
                </div>

                <div class="card-body">
                    <h3><%= list.title %></h3>
                    <p class="price">₹<%= list.price %> for 2 nights</p>
                    <p class="rating">⭐ <%= list.rating || "4.9" %></p>
                </div>

            </a>
        <% } %>
    </div> -->

